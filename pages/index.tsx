// The combined swing + scalp dashboard (defaults to swing mode) is now the
// landing page at "/". Its implementation lives in ./legacy, which remains
// reachable at /legacy as well.
export { default } from "./legacy";
