---
name: test-and-lint-roadmap
description: "Roadmap COMPLETE 2026-08-29: harness + contract nets + ESLint strict done — VANILLA config (no rule overrides, test/** excluded), 0 errors / 0 warnings; accepted cases carry documented inline disables"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1fd7c99e-9b5b-47f9-b10f-f4709464cd50
  modified: 2026-08-29T09:09:06.142Z
---

Roadmap agreed 2026-08-28 (tests BEFORE linting — lint autofixes over analyze.ts are exactly the churn the net must cover). Modeled on `C:\Users\MayessSammoud\OneDrive - SKR Reisen GmbH\Dokumente\git\skr3-zoho-functions`.

**DONE (session 2026-08-28 evening):**
1. ✅ Harness in `test/harness/`: `startBoundary(world|factory)` + ordered conversation recorder (HTTP via msw `onUnhandledRequest:'error'` + SQL via fake `PgClient` planted on `global.__pgClient` — NOT the SCALP_PG_USE_HTTP route), frozen clock (`vi.useFakeTimers({now: FIXED_NOW_MS, toFake:['Date']})` — only Date faked, timers real so retry/pacing sleeps still elapse), normalizers (ISO ts, FIXED_NOW epoch-ms, api_token, authorization/access-sign masks). World builders in `test/harness/worlds/`: bitget (code-00000 envelope), capital (session CST/X-SECURITY-TOKEN handshake), aiGateway (openAiDecides/claudeDecides canned shapes), kv (in-memory Redis, BOTH URL conventions: JSON-array POST at base + path-style /CMD/args), news (coindesk+marketaux), forexFactory. Env: `setup-env.ts` (scrub real creds + neutralize retries/pacing; note `KV_MAX_RETRIES='0'` would fall back to 3 — uses '1') and `setup-contract.ts` (import-time-frozen vars: KV URL, `SCALP_PG_CONNECTION_STRING` so isPgConfigured()=true). Two vitest projects: `unit` and `contract` (vitest.config.ts `test.projects`).
2. ✅ All 259 node:test tests migrated to `test/unit/` (mirrors lib/), `t.after`→`t.onTestFinished`; old `test:ts` script removed; scripts now `test`, `test:unit`, `test:contract`, `test:watch`. 270 tests green, tsc clean, ~3s.
3. ✅ Thin per-boundary contract nets (`test/contract/*.contract.test.ts`): kv, news, aiGateway (both providers, prompt-regression net), bitget, capital, pg, forexEvents.
4. ✅ (same session, later) Analyze-tick contract snapshots — `test/contract/analyze/{flat-hold,entry-sell,in-position-manage,flat-gated}.contract.test.ts`, shared world in `analyze/world.ts`, snapshots 69–153KB each (full tick conversation incl. prompt + SQL persistence trail). Market data replayed from REAL captures: `npm run test:fixtures:capture -- SYMBOL` (scripts/capture-analyze-fixtures.ts) real-fetches only public bitget /market/* endpoints, stubs all else, runs one dryRun tick through the actual pages/api/analyze.ts handler (there is NO lib-level runTick — handler needs only {method,url,query,headers} + chainable res.status().json(), helpers in test/harness/next.ts). Fixtures: bitget-BTCUSDT.json (actionable at capture → full-prompt scenarios; trend-DOWN so the entry scenario is SELL — a BUY gets demoted to HOLD by postprocess counter-trend rule ai.ts:1697) and bitget-ETHUSDT.json (boxed_or_unconfirmed → gated scenario). Tests freeze the clock at fixture.capturedAtMs via `startBoundary(world, {nowMs})`.
4b. ✅ Capital.com tick net (same session): `test/contract/analyze/capital-{flat-gated,manage,market-closed}.contract.test.ts` + `fixtures/capital-EURUSD.json`. Capture script now supports `SYMBOL capital category` — needs real CAPITAL_* creds (`vercel env pull`, read-only; session never recorded, account-state endpoints stubbed → fixtures are market data only). Fixture world generalized: `worlds/recordedMarkets.ts` (bitgetMarketWorld + capitalMarketWorld; bitgetFixtures.ts is GONE). Capital facts: flat/open decider = GET /api/v1/positions; bracket lives ON the position row (stopLevel/profitLevel, no plan orders); flat-only gates (actionability/extension/warmup/affordability) don't apply in-position → the MANAGE scenario carries the full capital prompt (9 capital symbols probed 2026-08-28 evening, all flat-gated); dryRun entry short-circuits with zero HTTP; HOLD+tp/sl runs updateCapitalPositionLevels (GET /positions, stops before PUT); prices calls carry only stable params (max+resolution). Hazard fixed: clientOid normalizer must not use leading \b (ids sit after %22 in URL-encoded KV bodies). Known cost: capital tick tests ~12s each — rate limiter (min 100ms/call, rps clamped ≤10 in lib) waits grow linearly under the frozen Date.

4c. ✅ (same session, final round) Execution + remaining surfaces: LIVE dryRun=false scenarios — `analyze/live-entry-sell` (set-margin-mode/set-leverage/place-order bodies w/ preset bracket, thread INSERT, break-trigger), `analyze/live-manage-amend` (thread transcript REPLAY into the AI call + modify-tpsl-order body), `analyze/capital-live-amend` (PUT /positions/{dealId} whole-bracket replacement); `analyze/event-blackout` (HIGH USD event in 30min pre-block, capital EURUSD); `analyze/cron-quarter` (x-vercel-cron + dryRun=false&decisionPolicy=balanced like vercel.json — hits the cron-only not_primary_close gate at 18:24 UTC, exercises kill-switch KV read, lastScan, warm-latch INCR as finisher 1/29); `test/contract/postmortem.contract.test.ts` (2nd prompt surface: forensic system prompt + dossier in snapshot; claim/complete/lesson-INSERT choreography; exit ≥12h before frozen now keeps candle endTime deterministic); `test/contract/evaluate.contract.test.ts` (3rd prompt surface; KV history seeded via real appendDecisionHistory — KV stores prompt:null so the PG hydration hop runs). Harness: createApiResponse now has setHeader; analyzePgWith() override factory. 51 files / 284 tests, all deterministic.

4d. ✅ wake-watch route netted (`test/contract/wake-watch.contract.test.ts` — band fire w/ snapshot, kill-switch suppression, venue-close detection, emergency move; `wake-watch-failed-break.contract.test.ts` — own clock at 08:05 UTC, 5min after a 4H close). Key trick: with SCALP_ORCHESTRATOR_BASE_URL/APP_BASE_URL/URL scrubbed, invokeCronEndpoint resolves from req.headers.host → the analyze SELF-FIRE (GET …/api/swing/analyze?…&wake=1) goes through msw and lands in the snapshot. Wake state is swapped per test via installFakePg(wakePg(state)) since the baseline db responder is fixed at startBoundary time. 53 files / 289 tests green.

5. ✅ Linting (session 2026-08-28 night): `eslint.config.mjs` flat config — includeIgnoreFile(.gitignore) + `stubs/**` ignored, eslint-config-next/core-web-vitals + /typescript (Next 16 native flat exports), tseslint.configs.strict, `project/overrides` block, `project/tests` block (test/** + vitest.config.ts: @vitest/eslint-plugin recommended; any/non-null-assertion/dynamic-delete off). Scripts `lint`/`lint:fix`. Deps: eslint 9.39, eslint-config-next 16.3.3, typescript-eslint 8.68, @eslint/compat, @vitest/eslint-plugin. Result: 0 errors / 620 warnings; tsc clean; 289 tests green after fixes.
   - Fixed in code: all unused vars/imports deleted as dead code (incl. the entire dead evaluate-job polling chain in pages/index.tsx — triggerEvaluation/pollEvaluationJob/evaluateJobs state/timers ref/types/JSX badge — and the dead close_conditions block in pages/api/analyze.ts), `x! ?? y` → `x ?? y` (indicators.ts), empty interfaces → type aliases (gates.ts), dynamic deletes → Reflect.deleteProperty, prefer-const autofixed. no-unused-vars configured with `^_` ignore patterns + ignoreRestSiblings.
   - FINAL CONFIG (user request 2026-08-29, commit 68314d2): eslint.config.mjs is VANILLA — includeIgnoreFile(.gitignore) + ignores ['test/**'] + nextVitals + nextTs + tseslint strict, nothing else (stubs/pg-cloudflare + its npm override + gitignore wrangler block removed entirely in ee4dee0 — vanilla pg now, its optional pg-cloudflare dep is inert on Vercel/Node). No rule overrides. test/** deliberately unlinted (user preference; note: airbnb never auto-excluded tests either — its dev-dependency thing is only import/no-extraneous-dependencies test globs). @vitest/eslint-plugin uninstalled. Accepted cases live as documented inline eslint-disable-next-line comments: 4× react-hooks/set-state-in-effect (index.tsx dashboard-load + symbol-details, ChartPanel chart-fetch + live-tick merge) and 6× @typescript-eslint/no-unused-vars (interface-parity stubs in capital.ts fetchCapitalRealizedRoi + db/client.ts $transaction; rest-omit destructures in claudeAi.ts + swing/pg.ts). loadDashboard now accepts opts.range (threaded from the refreshDashboard effect event). eslint: 0/0.
   - RATCHET COMPLETED (session 2026-08-29, commits 2b952a1 + 07be96a + 0ad5935): all 595 any/non-null warnings fixed in two parallel-agent waves (type-level only: unknown+narrowing at JSON boundaries, structural venue-payload/SQL-row types, `TradeDecision & Record<string, unknown>` decision flow, lightweight-charts structural "Like" views in ChartPanel). React hooks modernized by hand: theme + phone-layout via useSyncExternalStore module stores, reset-on-symbol-switch via render-time adjustment (prev-key pattern), useEffectEvent (React 19.2 stable) for admin headers/dashboard refresh/chart-init snapshot/applyPayload/warm-refresh (replaced latest-ref writes-in-render), week-calendar "now" stamped at data load (purity), overlay pane width via ResizeObserver state. any/non-null-assertion/react-hooks refs+purity now ERRORS again; set-state-in-effect stays warn for exactly 4 accepted canonical fetch-effects (index.tsx dashboard-load + symbol-details, ChartPanel chart-fetch + live-tick merge) — no rule-clean formulation without worse code. End state: 0 errors / 4 warnings, tsc clean, 289 tests green, next build passes.

6. Still optional/deferred: capital live entry via workingorders (needs an actionable flat capital capture), weekly-digest prompt surface.

Gotchas (validated): analyze.ts + capital.ts hold module state (persist map, session/epic caches, swingSchemaReady) → ONE scenario per test FILE; runAnalyzeTick warms the pg schema then resets the recorder so DDL stays out of snapshots; fixture matcher ignores startTime/endTime/after and compares productType case-insensitively; harness stubs Math.random (pg.ts 1-in-500 retention sweep) and normalizes cfw-<UUID> clientOids; actionability gate outcome is data-dependent — after any fixture re-capture, verify promptSkipped and the entry direction; manual-cadence ticks never hit the quiet/quarter gates (no x-vercel-cron header).
